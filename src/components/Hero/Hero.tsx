import React from 'react';
import styled from 'styled-components';
import {Link} from 'react-router-dom';

const Hero = (props) => {
    return (
        <div>lol</div>
    );
}

export default Hero;
